# Electron + React Todo App

**WIP**

A cross-platform desktop version of the Todo app using Electron and React.  
All data is saved locally using the Electron file system.

## Features
- Add/edit/delete tasks
- Optional details for each task
- Sort/filter
- Persistent local storage via JSON file